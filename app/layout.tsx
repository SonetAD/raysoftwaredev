import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Ray Software Development - AI-Powered Solutions & Products",
  description: "Ray is redefining everyday life through smart, intuitive AI built for real human impact. Creators of MyDressr and custom AI solutions tailored to your success.",
  keywords: ["AI Development", "Software Development", "MyDressr", "AI Fashion", "Wardrobe Management", "Machine Learning", "Custom Software"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
